[toolchain]

[features]
seeds = false
skip-lint = false

[programs.devnet]
fission_quest = ""

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = ""

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
